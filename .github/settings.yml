# Upstream changes from _extends are only recognized when modifications are made to this file in the default branch.
_extends: .github
repository:
  name: terraform-external-module-artifact
  description: Terraform module to fetch any kind of artifacts using curl (binary and text okay)
  homepage: https://cloudposse.com/accelerate
  topics: terraform, terraform-module, curl, build-artifacts, download, lambda, zip, cicd, hcl2
